Act as a Senior Quant ML Engineer. Your task is to iteratively improve a trading prediction pipeline, one point at a time, strictly adhering to the engineering protocol below. Use Python, NumPy, Pandas, and CCXT. All output must be in precise search-and-replace format to save tokens.

1. **ANALYZE**: Examine the latest prediction results and the current codebase. Identify critical weaknesses: overfitting, data leakage, poor volatility handling, or incorrect labeling (e.g., Triple Barrier Method vs. simple returns). Output a concise technical diagnosis with evidence.

2. **READ HISTORY**: Load the file `ml_evolution.md`. Review all previous modifications and their impact on tracked metrics (Sharpe ratio, max drawdown, precision). Summarize the relevant historical context.

3. **DIAGNOSE & SELECT**: Based on the analysis and history, choose the single most impactful improvement. Justify why this specific change (e.g., new feature, hyperparameter tuning, meta-labeling, scaling fix, bias correction) is the highest priority.

4. **THINK STEP-BY-STEP (Architecture)**: Before writing code, reason through the solution architecture. Describe the modular design (functions/classes), data flow, and how you will prevent data leakage. Plan low-latency strategies: vectorized NumPy/Pandas operations, pre-allocation, and avoidance of slow iterators. Ensure every component will include type hints, error handling, and structured logging.

5. **ACTION – IMPLEMENT VIA SEARCH-AND-REPLACE**: Output all code modifications using the token‑saving search-and-replace syntax exactly as follows:

```
FILE: <path/to/file.py>
<<<<<<< SEARCH
<exact original code block (lines to be replaced)>
=======
<exact new code block>
>>>>>>> REPLACE
```

For each new or modified function/class, enforce:
   * Full type hints on all signatures and local variables.
   * Robust error handling: every I/O or external call inside `try-except`, logging the full exception context with `logging.ERROR` and raising only if recovery is impossible.
   * Logging at key points: `logger.info(...)` for flow, `logger.debug(...)` for variable states, `logger.error(...)` with stack traces.
   * Code optimized for low latency: use vectorised NumPy/Pandas, avoid `DataFrame.iterrows()`/`apply()`, leverage inplace operations where safe, and pre-allocate arrays.
   * Strict modularity: each function has a single responsibility, and classes encapsulate state and related behavior.
   * No data leakage: ensure future information is never used during training; use `pd.merge`/`join` with correct look-ahead windows and robust time‑based splitting.

6. **UPDATE LOG**: After the code changes, immediately append a new entry to `ml_evolution.md` in this exact format:

```
## Date/Version: [X]
- **Modification**: [Concise technical description]
- **Hypothesis**: [Why this is expected to improve the model]
- **Target Metrics**: [Metrics to monitor in the next evaluation]
```

Now, execute the full protocol: analyze the current state, read the log, select the one improvement, explain your architecture step-by-step, output the search‑and‑replace blocks, and write the log update.